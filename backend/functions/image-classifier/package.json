{
  "name": "image-classifier",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "esbuild src/functions/*.ts --bundle --platform=node --target=node22 --outdir=dist/src/functions --log-level=info --external:@azure/functions --external:@azure/functions-core",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "start": "func start --port 7072",
    "dev": "concurrently --raw 'npm run watch' 'npm run start'",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "types:check": "tsc --noEmit"
  },
  "dependencies": {
    "@azure-rest/ai-vision-image-analysis": "^1.0.0-beta.3",
    "@azure/cosmos": "^4.8.0",
    "@azure/functions": "^4.0.0",
    "@azure/identity": "^4.13.0",
    "@azure/storage-blob": "^12.30.0",
    "axios": "^1.13.2",
    "doner_types": "file:../shared/doner_types"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "azure-functions-core-tools": "^4.x",
    "concurrently": "^9.2.1",
    "esbuild": "^0.27.2",
    "jest": "^29.5.0",
    "rimraf": "^6.1.2",
    "ts-jest": "^29.1.0",
    "typescript": "^5.9.3"
  }
}
